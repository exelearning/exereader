import 'package:flutter/material.dart';
import 'package:exereader/compose.dart';

/**
 *  This class simply launches the application by calling the runApp() method,
 *  which is passed as a parameter an instance of the class that assemblesall
 *  the widgets that make up the application.
 */

void main() {
  runApp(ExeReader());
}